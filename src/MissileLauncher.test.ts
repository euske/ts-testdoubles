import { Missile } from "./Missile"
import { LaunchCode } from "./LaunchCode"
import { MissileLauncher } from "./MissileLauncher"

describe('Missile Launcher Tests', () => {

    test('example', () => {
    //const launcher = new MissileLauncher()
    //const missile = new Missile()
    //const launchCode = new LaunchCode()
    //launcher.launchMissile(missile, launchCode)

    expect(1).toBe(2)
  })

})
